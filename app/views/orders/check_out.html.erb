<%= form_with model: @order, method: :post do |f| %>
  <div class="row">
  <div class="col-75">
    <div class="container-form">
      <form action="/action_page.php">

        <div class="row">
          <div class="col-50">
            <h3>Billing Address</h3>
<!--            example css code-->
<!--            <label for="fname"><i class="fa fa-user"></i> Full Name </label>-->
<!--            <input type="text" id="fname" name="firstname" >-->
            <%= f.label :name do %><i class="fa fa-user"></i> Full Name <% end %>
            <%= f.text_field :name, placeholder: "John M. Doe" %>
            <%= f.label :email do %> <i class="fa fa-envelope"></i> Email<% end %>
            <%= f.text_field :email, placeholder: "ada-lovelace@petsy.com", required: true %>
            <%= f.label :address do %><i class="fa fa-address-card-o"></i> Address <% end %>
            <%= f.text_field :address, placeholder: "542 W. 15th Street" %>
            <%= f.label :city do %><i class="fa fa-institution"></i> City<% end %>
            <%= f.text_field :city, placeholder: "Seattle" %>
            <div class="row">
              <div class="col-50">
                <%= f.label :state do %> State <% end %>
                <%= f.text_field :state, placeholder: "WA" %>
              </div>
              <div class="col-50">
                <%= f.label :zip_code do %> Zip <% end %>
                <%= f.text_field :zip_code, placeholder: "98101" %>
              </div>
            </div>
          </div>

          <div class="col-50">
            <h3>Payment</h3>
            <label for="fname">Accepted Cards</label>
            <div class="icon-container-form">
              <i class="fa fa-cc-visa" style="color:navy;"></i>
              <i class="fa fa-cc-amex" style="color:blue;"></i>
              <i class="fa fa-cc-mastercard" style="color:red;"></i>
              <i class="fa fa-cc-discover" style="color:orange;"></i>
            </div>

            <%= f.label :card_number do %> Credit card number<% end %>
            <%= f.text_field :card_number, placeholder: "1111-2222-3333-4444" %>
            <%= f.label :expiration_month do %> Expiration Month<% end %>
            <%= f.text_field :expiration_month, placeholder: "September" %>


            <div class="row">
              <div class="col-50">
                <%= f.label :expiration_year do %> Expiration Year<% end %>
                <%= f.text_field :expiration_year, placeholder: "2022" %>
              </div>
              <div class="col-50">
                <%= f.label :security_code do %> Security Code<% end %>
                <%= f.text_field :security_code, placeholder: "352"%>
              </div>
            </div>
          </div>
        </div>
        <%= f.submit 'Make Purchase', class: "btn" %>
      </form>
    </div>
  </div>
</div>
<% end %>
